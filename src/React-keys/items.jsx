import React from 'react'

const Items = () => {
    const items=['apple','banana','orange']
  return (
    <div>
      <ul>
        {items.map((item ,index)=>(
        
        ))}
      </ul>
    </div>
  )
}

export default Items
